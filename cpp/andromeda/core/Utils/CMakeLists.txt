cmake_minimum_required(VERSION 3.24)

project(Utils)

set(CMAKE_BUILD_PARALLEL_LEVEL)
set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)


# SPDLog


# ==== FileOperations ====
set(FILE_OPERATIONS_HEADERS
    Utils/FileOperations/include/FileOperations.hpp
)

set(FILE_OPERATIONS_SOURCES
    Utils/FileOperations/src/FileOperations.cpp
)

# ==== nlohmann ====
set(NLOHMANN_HEADERS
    Utils/nlohmann_json/include/json.hpp
)

# ==== stb ====
set(STB_HEADERS
    Utils/stb/include/stb_easy_font.h
)


# Create a library target
add_library(${PROJECT_NAME} 
    STATIC
        ${FILE_OPERATIONS_HEADERS}
        ${FILE_OPERATIONS_SOURCES}
        ${NLOHMANN_HEADERS}
        ${STB_HEADERS}
)

target_include_directories(${PROJECT_NAME} 
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/
)

set_target_properties(${PROJECT_NAME} PROPERTIES POSITION_INDEPENDENT_CODE ON)

# Link spdlog
target_link_libraries(${PROJECT_NAME}
    PRIVATE
        PrecompiledHeaders
        spdlog::spdlog
)


source_group(
    TREE 
        ${CMAKE_CURRENT_SOURCE_DIR}/${PROJECT_NAME}/FileOperations
    PREFIX
        "FileOperations"
    FILES
        ${FILE_OPERATIONS_HEADERS}
        ${FILE_OPERATIONS_SOURCES}
)

source_group(
    TREE 
        ${CMAKE_CURRENT_SOURCE_DIR}/${PROJECT_NAME}/nlohmann_json
    PREFIX
        "nlohmann_json"
    FILES
        ${NLOHMANN_HEADERS}
)

source_group(
    TREE 
        ${CMAKE_CURRENT_SOURCE_DIR}/${PROJECT_NAME}/stb
    PREFIX
        "stb"
    FILES
        ${STB_HEADERS}
)